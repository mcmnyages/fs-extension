{
  "name": "bloglist",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix bloglist_backend\" \"npm run dev --prefix bloglist-frontend\"",
    "build": "npm run build --prefix client",
    "start": "NODE_ENV=production npm start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  },
  "author": "@McMnyages",
  "license": "ISC"
}
